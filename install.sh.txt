#!/usr/bin/env bash

devide=$' \n'"---------------------------------------------------------------------"$'\n'

echo "$devide"

sudo apt update
sudo apt install mysql-server
sudo mysql_secure_installation --host=0.0.0.0 --port=3307 --password=happycloud63 
sudo apt install php libapache2-mod-php php-mysql

sudo apt update
sudo apt install phpmyadmin php-mbstring php-zip php-gd php-json php-curl
sudo phpenmod mbstring
sudo systemctl restart apache2

sudo mysql
ALTER USER 'gogreen'@'%' IDENTIFIED WITH mysql_native_password BY 'happycloud63';
GRANT ALL PRIVILEGES ON *.* TO 'gogreen'@'%' WITH GRANT OPTION;
exit

